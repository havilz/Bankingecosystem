<Page x:Class="BankingEcosystem.Atm.UI.Views.HistoryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:BankingEcosystem.Atm.UI.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}"
      Title="Mutasi Rekening"
      Background="{StaticResource DarkNavyBrush}"
      Focusable="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header -->
            <RowDefinition Height="*" />    <!-- Content -->
            <RowDefinition Height="Auto" /> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Padding="32,16" Background="{StaticResource NavyBrush}">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="36" Height="36" Fill="{StaticResource GoldBrush}" Margin="0,0,12,0" />
                <TextBlock Text="Bank Ecosystem - Mutasi Rekening (Mini Statement)"
                           FontSize="18" FontWeight="Bold"
                           Foreground="{StaticResource WhiteBrush}"
                           VerticalAlignment="Center"
                           FontFamily="{StaticResource PrimaryFont}" />
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Row="1" Margin="40">
            
            <TextBlock Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" 
                       Text="Memuat Riwayat Transaksi..." 
                       FontSize="20" Foreground="#80FFFFFF" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <TextBlock Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}" 
                       Text="{Binding ErrorMessage}" 
                       FontSize="20" Foreground="#FF5555" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <ListView ItemsSource="{Binding Transactions}" 
                      Background="Transparent" BorderThickness="0"
                      HorizontalContentAlignment="Stretch"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Tanggal" Width="180">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat='dd MMM HH:mm'}" Foreground="White" FontSize="16"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Tipe" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TransactionType}" Foreground="#B0B0B0" FontSize="16"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Keterangan" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}" Foreground="#D0D0D0" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Nominal" Width="180">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Amount, StringFormat='Rp {0:N0}'}" FontWeight="Bold" Foreground="{StaticResource GoldBrush}" FontSize="16" TextAlignment="Right"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="8,12"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Grid>

        <!-- Bottom Bar -->
        <Border Grid.Row="2" Padding="32,20" Background="#10FFFFFF">
            <Button Content="KEMBALI" Command="{Binding BackCommand}" 
                    Style="{StaticResource AtmSecondaryButtonStyle}" 
                    HorizontalAlignment="Center" Width="150"/>
        </Border>
    </Grid>
</Page>
