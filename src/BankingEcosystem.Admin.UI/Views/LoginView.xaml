<UserControl x:Class="BankingEcosystem.Admin.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BankingEcosystem.Admin.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="{StaticResource LightGrayBrush}">
        <!-- Centered Login Card -->
        <Border Background="White" Width="420" VerticalAlignment="Center" CornerRadius="12" Padding="40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.08" Color="Black"/>
            </Border.Effect>

            <StackPanel>
                <!-- Header -->
                <TextBlock Text="🏦" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Bank Ecosystem" FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource CorporateBlueBrush}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                <TextBlock Text="Office System — Employee Login" FontSize="13"
                           Foreground="{StaticResource SubtextBrush}" HorizontalAlignment="Center" Margin="0,0,0,32"/>

                <!-- Employee Code -->
                <TextBlock Text="Employee Code" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding EmployeeCode, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,16"/>

                <!-- Password -->
                <TextBlock Text="Password" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,6"/>
                <PasswordBox x:Name="PasswordBox"
                             Style="{StaticResource InputPasswordBoxStyle}" Margin="0,0,0,8"
                             PasswordChanged="PasswordBox_PasswordChanged"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}"
                           TextWrapping="Wrap" Margin="0,8,0,16" FontSize="13" HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Login Button -->
                <Button Command="{Binding LoginCommand}"
                        Height="44" IsDefault="True">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                            <Setter Property="Content" Value="Login"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Content" Value="Memproses..."/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Busy Indicator -->
                <TextBlock Text="Memverifikasi kredensial..." FontSize="12"
                           Foreground="{StaticResource SubtextBrush}" HorizontalAlignment="Center" Margin="0,12,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
