<UserControl x:Class="BankingEcosystem.Admin.UI.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:BankingEcosystem.Admin.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="{StaticResource LightGrayBrush}" Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- DataGrid -->
            <RowDefinition Height="Auto"/> <!-- Pagination -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Audit Logs" FontSize="24" FontWeight="Bold" 
                   Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,24"/>

        <!-- DataGrid -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="0" ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.05"/>
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" 
                      Style="{StaticResource FlatDataGrid}" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Waktu" Binding="{Binding CreatedAt, StringFormat='dd MMM yyyy HH:mm:ss'}" Width="160"/>
                    <DataGridTextColumn Header="User" Binding="{Binding EmployeeName, TargetNullValue='System'}" Width="120"/>
                    <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="150"/>
                    <DataGridTextColumn Header="Entity" Binding="{Binding EntityType}" Width="100"/>
                    <DataGridTextColumn Header="ID" Binding="{Binding EntityId}" Width="60"/>
                    <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Pagination -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Previous" Command="{Binding PreviousPageCommand}" Width="80" Margin="0,0,8,0"
                    IsEnabled="{Binding CanGoPrevious}"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            <TextBlock Text="{Binding Page, StringFormat='Page {0}'}" 
                       VerticalAlignment="Center" Margin="8,0"/>
            <!-- No Total Pages known -->
            <Button Content="Next" Command="{Binding NextPageCommand}" Width="80"
                    IsEnabled="{Binding CanGoNext}"
                    Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.Row="1" Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" 
              d:Visibility="Collapsed">
            <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Foreground="#666"/>
        </Grid>
    </Grid>
</UserControl>
