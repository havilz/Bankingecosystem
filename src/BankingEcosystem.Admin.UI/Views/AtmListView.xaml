<UserControl x:Class="BankingEcosystem.Admin.UI.Views.AtmListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BankingEcosystem.Admin.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:AtmListViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="ATM Management" FontSize="24" FontWeight="Bold" Foreground="#333"/>
            
            <Button Grid.Column="1" 
                    Content="+ Add ATM" 
                    Command="{Binding NavigateToAddCommand}"
                    Padding="15,8" 
                    Background="#007ACC" 
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"/>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1">
            <!-- Loading -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" 
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Loading ATMs..." FontSize="16" Foreground="#666"/>
            </StackPanel>

            <!-- Error -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" 
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="16" Margin="0,0,0,10"/>
                <Button Content="Retry" Command="{Binding LoadAtmsCommand}" Padding="10,5"/>
            </StackPanel>

            <!-- Data List -->
            <DataGrid ItemsSource="{Binding Atms}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="White"
                      BorderThickness="1"
                      BorderBrush="#EEE"
                      RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding AtmCode}" Width="100"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="*"/>
                    
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Offline"/>
                                            <Setter Property="Foreground" Value="#EF4444"/> <!-- Red -->
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                    <Setter Property="Text" Value="Online"/>
                                                    <Setter Property="Foreground" Value="#10B981"/> <!-- Green -->
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Total Cash" Binding="{Binding TotalCash, StringFormat=Rp {0:N0}}" Width="150"/>
                    <DataGridTextColumn Header="Last Refill" Binding="{Binding LastRefill}" Width="150"/>

                    <DataGridTemplateColumn Header="Actions" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Refill" 
                                            Command="{Binding DataContext.ShowRefillCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding AtmId}"
                                            Margin="0,0,10,0" Padding="10,5" Background="#28a745" Foreground="White" BorderThickness="0"/>
                                    
                                    <Button Content="Toggle Status" 
                                            Command="{Binding DataContext.ToggleStatusCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding AtmId}"
                                            Padding="10,5" Background="#ffc107" Foreground="Black" BorderThickness="0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
