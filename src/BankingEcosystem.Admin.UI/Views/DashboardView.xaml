<UserControl x:Class="BankingEcosystem.Admin.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BankingEcosystem.Admin.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="{StaticResource LightGrayBrush}" Margin="32">
        <StackPanel VerticalAlignment="Top">
            <!-- Welcome Header -->
            <TextBlock FontSize="28" FontWeight="Bold" Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,4">
                <Run Text="Selamat Datang, "/>
                <Run Text="{Binding EmployeeName}"/>
                <Run Text=" 👋"/>
            </TextBlock>
            <TextBlock FontSize="14" Foreground="{StaticResource SubtextBrush}" Margin="0,0,0,32">
                <Run Text="{Binding EmployeeRole}"/>
                <Run Text=" — "/>
                <Run Text="{Binding CurrentDate}"/>
            </TextBlock>

            <!-- Placeholder Stat Cards -->
            <TextBlock Text="Dashboard Overview" FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,16"/>

            <UniformGrid Columns="4" Margin="0,0,0,24">

                <!-- Card 1: Total Nasabah -->
                <Border Background="White" CornerRadius="10" Padding="24" Margin="0,0,12,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.04"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="👥" FontSize="28" Margin="0,0,0,8"/>
                        <TextBlock Text="Total Nasabah" FontSize="13" Foreground="{StaticResource SubtextBrush}"/>
                        <TextBlock Text="{Binding Stats.TotalCustomers}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource CorporateBlueBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card 2: Total Transaksi Hari Ini -->
                <Border Background="White" CornerRadius="10" Padding="24" Margin="0,0,12,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.04"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="💳" FontSize="28" Margin="0,0,0,8"/>
                        <TextBlock Text="Transaksi Hari Ini" FontSize="13" Foreground="{StaticResource SubtextBrush}"/>
                        <TextBlock Text="{Binding Stats.TotalTransactionsToday}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource AccentBlueBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card 3: Total ATM -->
                <Border Background="White" CornerRadius="10" Padding="24" Margin="0,0,12,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.04"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="🏧" FontSize="28" Margin="0,0,0,8"/>
                        <TextBlock Text="Total ATM" FontSize="13" Foreground="{StaticResource SubtextBrush}"/>
                        <TextBlock Text="{Binding Stats.TotalAtms}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Card 4: Total Aset -->
                <Border Background="White" CornerRadius="10" Padding="24">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.04"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="💰" FontSize="28" Margin="0,0,0,8"/>
                        <TextBlock Text="Total Simpanan" FontSize="13" Foreground="{StaticResource SubtextBrush}"/>
                        <!-- StringFormat for Currency (Rp) -->
                        <TextBlock Text="{Binding Stats.TotalBalanceStored, StringFormat='Rp {0:N0}'}" FontSize="20" FontWeight="Bold" Foreground="#854d0e" Margin="0,10,0,0"/> 
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Quick Info -->
            <Border Background="White" CornerRadius="10" Padding="24">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.04"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="ℹ️  Informasi" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,8"/>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource SubtextBrush}" LineHeight="22">
                        Dashboard ini akan menampilkan data real-time setelah fitur Customer Management dan ATM Management diimplementasikan.
                        Saat ini Anda dapat menggunakan menu navigasi di atas untuk berpindah antar fitur (setelah diimplementasikan di step selanjutnya).
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
