<UserControl x:Class="BankingEcosystem.Admin.UI.Views.AddAtmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BankingEcosystem.Admin.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:AddAtmViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Border Background="White" Width="500" VerticalAlignment="Center" HorizontalAlignment="Center" 
                Padding="30" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Register New ATM" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                <TextBlock Text="ATM Code" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding AtmCode, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,15"/>

                <TextBlock Text="Location" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,15"/>

                <TextBlock Text="Initial Cash (Rp)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding InitialCash, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Command="{Binding CancelCommand}" 
                            Width="100" Margin="0,0,10,0" Padding="8" Background="#EEE" BorderThickness="0"/>
                    
                    <Button Content="Save" Command="{Binding SaveCommand}" 
                            Width="100" Padding="8" Background="#007ACC" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
